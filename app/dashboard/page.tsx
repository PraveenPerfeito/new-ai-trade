import { MarketScanner } from '@/components/dashboard/market-scanner';

export const metadata = {
  title: 'Dashboard — Crypto Market Scanner',
};

export default function DashboardPage() {
  return <MarketScanner />;
}
